{
  "name": "chatwork-emojii",
  "version": "1.0.0",
  "private": true,
  "description": "Chatwork sticker extension",
  "scripts": {
    "sync:extension": "bash scripts/sync-extension.sh",
    "validate:data": "node scripts/validate-data.js",
    "check:js": "node --check scripts/content.js && node --check scripts/popup.js && node --check scripts/validate-data.js",
    "validate": "npm run validate:data && npm run check:js"
  }
}
